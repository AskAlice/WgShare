<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <!-- Only apps signed with our key (i.e. the :xposed hook APK) may push captured clipboard text. -->
    <permission
        android:name="dev.alice.wgshare.permission.CLIPBOARD_PUSH"
        android:protectionLevel="signature" />
    <!-- Held so we may push clipboard to the KDE Connect hook's guarded receiver (same key). -->
    <uses-permission android:name="dev.alice.wgshare.permission.CLIPBOARD_PUSH" />

    <!-- Package visibility (Android 11+) so we can broadcast to the local KDE Connect app. -->
    <queries>
        <package android:name="org.kde.kdeconnect_tp" />
    </queries>

    <application
        android:name=".WgShareApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.WgShare">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.WgShare">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.SyncService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Receives clipboard text captured by the SwiftKey LSPosed hook (:xposed module). -->
        <receiver
            android:name=".core.ClipboardPushReceiver"
            android:exported="true"
            android:permission="dev.alice.wgshare.permission.CLIPBOARD_PUSH">
            <intent-filter>
                <action android:name="dev.alice.wgshare.action.CLIPBOARD_PUSH" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
